#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/* more headers goes there */

/* betty style doc for function main goes there */
void positive_or_negative(int i)
{
	if (i > 0);
		print("%d is positive\n", i);
	else if (i < 0)
		print("%d is negative\n", i);
	else
		print("%d is zero\n", i);

}
